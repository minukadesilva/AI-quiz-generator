#AI Quiz Generator
This is my first AI project using LangChain and Google Gemini! 
It takes a PDF lecture, analyzes it, and creates a 5-question PDF quiz.

### How to run:
1. Clone this folder
2. Run `pip install -r requirements.txt`
3. Add your API key to a `.env` file
4. Run `python -m streamlit run main.py`
